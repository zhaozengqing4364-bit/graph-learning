---
argument-hint: [issue-id or focus]
description: 安全单点迭代 {{PROJECT_NAME}}：从审计清单中一次只处理一个高价值问题，改后立刻验证并记录
---

使用 `.claude/skills/safe-grow/SKILL.md` 作为本命令的工作流真源，并严格遵守项目 `CLAUDE.md`。

执行规则：

1. 先读取以下文件：
   - `CLAUDE.md`
   - `.claude/skills/safe-grow/SKILL.md`
   - `.claude/loop/PROJECT_GROWTH.md`
   - `.claude/loop/GLM_AUDIT.md`
   - `.claude/loop/GROWTH_BACKLOG.md`
   - `.claude/loop/state.json`
   - `.claude/loop/log.md`

2. 如果 `.claude/loop/PROJECT_GROWTH.md` 仍是模板或没有明确项目北极星，不要直接改代码。先补齐项目主目标、主流程、非目标和优先优化方向。

3. 如果 `.claude/loop/GLM_AUDIT.md` 仍是占位模板或没有明确问题清单，不要直接改代码。先要求用户补充或粘贴审计结果。

4. 先检查 `state.json`：
   - 如果存在未完成、验证失败、或待回滚的问题，优先继续该问题。
   - 如果 `GLM_AUDIT.md` 里仍有未完成问题，保持在 `stabilize` 模式，只能从审计问题中选择唯一一个最高优先级且最符合项目主价值链的问题。
   - 如果审计问题已全部完成，则切换到 `grow` 模式，从 `GROWTH_BACKLOG.md` 中选择唯一一个最高价值增长项。
   - 如果传入了 `$ARGUMENTS`，仅当它与当前模式中的待处理项一致时才优先处理；否则说明冲突并给出推荐项。

5. 选择问题时，必须额外做一层“复利优化筛选”：
   - 是否更有助于用户完成核心任务
   - 是否增强系统主能力，而不是仅做表层美化
   - 是否让后续迭代更安全、更容易验证、更容易定位问题
   如果不满足以上任一方向中的至少一个，就降级优先级或暂缓处理。

6. 当进入 `grow` 模式时，若 `GROWTH_BACKLOG.md` 为空、仍是模板、或候选质量太低，先做一次“增长扫描”，再开始改代码。增长扫描必须围绕以下方向生成 3-5 个候选项，并选出一个当前最值得做的项：
   - 提升用户完成核心任务的清晰度、速度、成功率
   - 增强项目的主价值闭环
   - 提升反馈质量、诊断质量、恢复能力、可观测性或测试保护
   - 降低高频手动成本和重复出错点
   每个候选必须写清楚：用户收益、系统能力收益、证据、最小改动、成功信号、验证方式。

7. 修改前必须明确输出：
   - 当前模式：`stabilize` 或 `grow`
   - 当前问题
   - 优先级
   - 为什么这件事值得现在做
   - 将修改的文件
   - 明确不修改的部分
   - 最小改动理由
   - 验证方案
   - 成功信号

8. 代码修改要求：
   - 一次只处理一个问题
   - 只改最小必要代码块
   - 保持当前代码风格、目录结构、依赖体系、命名习惯
   - 禁止顺手重构
   - 禁止扩散到无关模块
   - 如果涉及行为修复或功能变更，在可行时先写最小失败测试或最小失败验证，再写实现

9. 修改后必须立即验证：
   - 优先使用项目已有测试、构建、lint、类型检查、局部运行验证
   - 如果没有自动化验证，则执行最小必要手动验证，并明确覆盖范围与未覆盖风险
   - 验证失败时，不进入下一问题；先修当前问题或回退当前问题
   - 在 `grow` 模式下，除了功能正确，还要判断“成功信号是否被满足或至少被更接近”

10. 每轮结束都要更新：
   - `.claude/loop/state.json`
   - `.claude/loop/log.md`
   - 若发现新的高价值增长点，补充到 `.claude/loop/GROWTH_BACKLOG.md`
   - 若全部问题处理完成且增长项也清空，再生成或更新 `.claude/loop/final_review.md`

11. 输出格式固定为：

【当前轮次】
- 当前模式：
- 当前问题：
- 优先级：
- 本轮目标：

【价值判断】
- 用户收益：
- 系统能力收益：
- 是否属于复利优化：
- 成功信号：

【修改前确认】
- 将修改的文件：
- 将修改的代码范围：
- 明确不修改的部分：
- 最小改动理由：
- 验证方案：

【执行修改】
- 实际修改内容摘要：
- 是否出现额外联动：
- 若有，原因：

【修改后验证】
- 验证命令：
- 验证结果：
- 成功信号状态：
- 是否确认问题已解决：
- 是否确认未破坏原有功能：

【变更记录】
- 文件清单：
- 回滚定位：
- 风险备注：

【下一步】
- 是否进入下一轮：
- 下一轮建议问题：
